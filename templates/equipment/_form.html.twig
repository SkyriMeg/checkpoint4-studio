{{ form_start(form) }}
{{ form_widget(form) }}

{% if originalPicture is defined %}
	<i class="bi bi-image me-2"></i>
	<em>Image déjà enregistrée :
		{{ originalPicture }}</em>
{% endif %}
<br/><button class="btn button-ok mt-3">
	<i class="bi bi-check-lg me-2"></i>
	<span>{{ button_label|default('Enregistrer') }}</span>
</button>
{{ form_end(form) }}
